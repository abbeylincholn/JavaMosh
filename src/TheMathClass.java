public class TheMathClass {
    public static void main (String[] args) {
        // they are mathetical operations
        // int result = Math.round(1.1F); // one method to 1
        // int result = (int)Math.ceil(1.1F); // another method to 2
        // int result = (int)Math.floor(1.1F); // to 1
        // int result = Math.max(1, 2); // max is 2 or min in 1
        //System.out.println(result);
    }
}
